
BeeExpress:
  mysql:
    admin:
      database: BeeExpress_admin_dev       # 根据环境更改
      driverClassName: ${BeeExpress.mysql.driverClassName}
      host: ${BeeExpress.mysql.host}
      password: ${BeeExpress.mysql.password}
      port: ${BeeExpress.mysql.port}
      url: jdbc:mysql://${BeeExpress.mysql.admin.host}:${BeeExpress.mysql.admin.port}/${BeeExpress.mysql.admin.database}?serverTimezone=CTT&characterEncoding=utf8&useUnicode=true
      username: ${BeeExpress.mysql.username}
    driverClassName: com.mysql.jdbc.Driver
    host: BeeExpress.mysql.host             # 根据环境更改
    port: 3306                  # 根据环境更改
    username: root              # 根据环境更改
    password: lzl123

spring:
  application:
    name: BeeExpress-auth-server
  sleuth:
    sampler:
      percentage: 1
    integration:
      enabled: false
    scheduled:
      skip-pattern: "^org.*HystrixStreamTask$"
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
  redis:
    key-prefix: dev
    host: 192.168.1.118
    pool:
      max-active: 100
      min-idle: 2
      max-idle: 8
      max-wait: 100
  #rabbitmq:
   # host: BeeExpress.rabbitmq.host
    #port: 5672
    #username: guest
    #password: guest
  zipkin:
#    base-url: http://BeeExpress-zipkin:8510
    locator:
      discovery:
        enabled: true
    service:
      name: ${spring.application.name}
#    rabbitmq:
#      queue: zipkin


#ribbon:
 # eureka:
  #  enabled: true
  #ReadTimeout: 60000
  #ConnectTimeout: 60000
  #MaxAutoRetries: 0
  #MaxAutoRetriesNextServer: 1
  #OkToRetryOnAllOperations: false

#hystrix:
  #threadpool:
  #  default:
    #  coreSize: 1000 # 并发执行的最大线程数，默认10
    #  maxQueueSize: 1000 # BlockingQueue的最大队列数
     # queueSizeRejectionThreshold: 500 # 即使maxQueueSize没有达到，达到queueSizeRejectionThreshold该值后，请求也会被拒绝
  #command:
   # default:
    #  execution:
       # isolation:
         # thread:
          #  timeoutInMilliseconds: 5000

eureka:
  instance:
    status-page-url-path: ${management.context-path}/info
    health-check-url-path: ${management.context-path}/health
    preferIpAddress: true
    status-page-url: http://gateway.BeeExpress.com:9770/api/auth/swagger-ui.html
    ip-address: 127.0.0.1
    metadata-map:
      user.name: ${security.user.name}
      user.password: ${security.user.password}
      management.context-path: ${management.context-path}


management:
  security:
    enabled: true
  context-path: /bee

security:
  basic:
    enabled: true
    path: ${management.context-path}
  user:
    name: admin
    password: ${BeeExpress_AUTH_SERVER_PASSWORD:admin}
endpoints:
  health:
    enabled: true
    sensitive: true  #actuator的health接口是否需要安全保证

